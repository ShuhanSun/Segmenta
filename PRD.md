# Segmenta 产品说明（提交前需真人重写/确认）

> 状态：工程事实草稿，不是合格的“人工撰写 PRD”。这份文字由 Codex 根据本仓库的真实实现整理，不能冒充真人作品提交。拟定产品负责人 Shuhan Sun 需要亲自运行产品、修改为自己的表述，并在文末填写确认记录后，才能把 `META.yaml` 中的 PRD 状态改为 `human-authored`。

## 产品是干什么的

Segmenta 是一个嵌入本地进程的结构化事件存储，也提供命令行工具。它适合测试工具、桌面程序、小型服务或离线任务保存 JSON 事件，并在没有数据库服务器、账号或公网的环境中完成恢复、过滤、聚合和数据保留。

真人产品负责人已经将主要场景收窄为 Coding Agent 执行追踪，并要求保留完整开发轨迹。以下是其未经润色的原始问题陈述：

> 提供一个同一个存储和查询工具，用于跨agaent，跨对话，分散的多轮对话和工作。
> 现有聊天记录分散保存在各自的平台或工具中，git只保存代码修改没有与agent的对话记录。

因此，后续版本需要在通用事件存储内核之上定义会话、用户消息、Agent 回复、工具调用、代码变更、测试结果、性能读数和跨会话关联等领域模型。当前 `v0.1.0` 只完成通用存储内核，还没有完成这些 Agent 专用模型和导入器。

它把事件追加到带 CRC32 校验的 segment 文件。事件最少包含数值时间戳 `timestamp`、非空类型 `type` 和 JSON 对象 `data`；系统为没有 ID 的事件生成 ID。Segmenta 不提供网络 API、跨机器复制、复杂事务、任意表达式查询或强一致快照。

## 核心功能

### F1. 批量追加、分段与尾部恢复

调用方可以批量追加事件。整批事件会先完成结构和 JSON 值验证；任一事件不合法时，该批次不会写入前缀。写入保持批次顺序，为缺少 ID 的事件生成 ID，并在达到配置大小时滚动到新 segment。默认写入完成后执行 `fsync`，批量导入可明确关闭同步。

每个事件帧包含 CRC32。正常读取遇到损坏会失败，而 `recover` 可以报告首个损坏位置；显式 `repair=True` 时只截断损坏位置及其后的文件尾部，已经完整写入的帧保持不变。它不尝试修复 segment 中间的任意损坏。

### F2. 过滤查询与游标分页

调用方可以按事件类型、闭区间时间范围以及一个或多个 `data.<path>` 精确值过滤事件。结果保持物理追加顺序。单页上限为 10,000 条；有下一页时返回不透明游标，继续读取时不重复、不遗漏。

游标绑定原查询条件，拿去执行另一条查询会失败。压缩会重写物理 segment，因此压缩前签发的游标不保证继续有效；不存在的字段不匹配普通值。

### F3. 流式分组聚合

调用方可以在与查询相同的类型、时间和 `data` 精确过滤之上执行 `count`、`sum`、`min`、`max`、`avg`，并可按 `type`、`timestamp`、`id` 或 `data.<path>` 分组。聚合以流式扫描运行，不把全部事件加载到内存。

除 `count` 外，聚合必须指定数值字段。布尔值、缺失值和非数值值会被跳过。当前版本不支持百分位、去重计数或窗口聚合。

### F4. 压缩与按时间保留

调用方可以重写 segment 来合并物理布局，也可以给出 `retain_after`，删除时间戳早于该值的事件。保留下来的事件顺序、内容和 ID 不变。操作返回压缩前后事件数与字节数。

压缩期间写入被排他锁阻塞。当前版本面向单机受控运维，不承诺读者与压缩并发时的快照一致性；压缩也不是跨进程崩溃事务。

### F5. 完整命令行工作流

`segmenta` 提供 `init`、`append`、`query`、`aggregate`、`compact`、`recover`、`stats` 子命令。输入事件使用 JSON Lines，输出使用单个 JSON 对象，便于 shell 或其他程序消费。无效 JSON 会报告源行号；错误以非零状态退出。

## 基本架构

- `models.py` 定义公开事件结构与输入边界。
- `codec.py` 在事件与“CRC + 紧凑 JSON + 换行”的磁盘帧之间转换。
- `storage.py` 管理目录、跨进程写锁、segment 滚动、顺序读取和尾部恢复。
- `query.py` 扫描 segment，应用过滤并签发绑定查询条件的物理游标。
- `aggregate.py` 在顺序事件流上维护小型分组累加器。
- `lifecycle.py` 把保留事件流式写入临时 segment，再切换文件。
- `cli.py` 只负责解析输入和序列化输出，业务行为复用上述库 API。

运行时只有一个 Python 进程，不需要 GPU、服务端、外部账号或网络。写入路径为：输入 → `Event` 验证 → 帧编码 → 文件锁 → segment；读取路径为：segment → CRC/JSON 解码 → 过滤 → 分页或聚合。

## 如何运行

```bash
python3 -m venv .venv
.venv/bin/python scripts/install.py
./scripts/verify.sh
./scripts/bench.sh --sizes 10000 50000 100000 --repeats 5
```

## 明确不做的事

Segmenta 不是数据库服务器，不提供 SQL、身份认证、多机复制、加密、索引、备份上传或云端管理。CRC 用于意外损坏检测，不是防篡改安全机制。

## 真人作者确认（当前未完成）

- 姓名：待真人填写
- 角色及与仓库关系：待真人填写
- 我已逐项运行并理解 F1–F5：`否（待完成）`
- 我已用自己的话重写本文，而非仅接受模型草稿：`否（待完成）`
- 日期：待填写

真人原始决策保存在 `docs/HUMAN_PRODUCT_DECISIONS.md`。这些回答证明产品定位由真人选择，但尚不足以把整份 PRD 标记为真人撰写。
